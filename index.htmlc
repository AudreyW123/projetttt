<html>
    <head>
        <title>what console screen do</title>
    </head>
    <p> Console Screen is a combination of a display monitor and an input device, usually a keyboard and mouse pair, which allows a user to input commands and receive visual output from a computer or computer system.
    Here is an example.</p>

<body>
    <h1>
        Open console
    </h1>
    <script>
        console.log("Hi I am console screen");
        var first_name="Rockey"
        console.log(first_name);
        var last_name="singh";
        console.log(last_name);
        var full_name= first_name.concat(last_name);
        console.log(full_name);
        var length_of_full_name=full_name.length;
        console.log(length_of_full_name);
        var name_one="I am Audrey Wong";
        console.log(name_one);
        var replace_name= name_one.replace("Audrey","wong");
        console.log(replace_name);
        var capital_name="ROCKY";
        var makig_lower_case= capital_name.toLowerCase();
        console.log(replace_name);
        var small_name="audrey";
        var making_lower_case= small_name.toLowerCase();
        console.log(making_lower_case);
        var square_root= Math.sqrt(25);
        console.log(square_root);
        var maximum_number= Math.max(15,10);
        console.log(maximum_number);
        var minimum_number= Math.min(15,10);
        console.log(minimum_number);
        var power_eg_1 = Math.pow(4,2);
        console.log(power_eg_1);
        var power_eg_2 = Math.pow(4,3);
        console.log(power_eg_2);

    </script>
    </body>
    </html>